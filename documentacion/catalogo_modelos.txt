📘 1) CATÁLOGO DE MODELOS — FantasyPro (Actualizado a mod0013)


Modelo: Liga (liga.dart)
Constructor
const Liga({
  required this.id,
  required this.nombre,
  required this.temporada,
  required this.descripcion,
  required this.fechaCreacion,
  required this.activa,
});

Métodos

factory Liga.desdeMapa(String id, Map<String, dynamic> datos)

Map<String, dynamic> aMapa()

Liga copiarCon({ ... })

Campos

Obligatorios: id, nombre, temporada, descripcion, fechaCreacion, activa
Opcionales: ninguno

Ejemplo
final liga = Liga(
  id: "L1",
  nombre: "Liga Pro",
  temporada: "2024/2025",
  descripcion: "Competencia oficial",
  fechaCreacion: 1700000000000,
  activa: true,
);

Modelo: ParticipacionLiga (participacion_liga.dart)
Constructor
const ParticipacionLiga({
  required this.id,
  required this.idLiga,
  required this.idUsuario,
  required this.nombreEquipoFantasy,
  required this.puntos,
  required this.fechaCreacion,
  required this.activo,
});

Métodos

factory ParticipacionLiga.desdeMapa(String id, Map<String, dynamic> datos)

Map<String, dynamic> aMapa()

ParticipacionLiga copiarCon({ ... })

Campos

Obligatorios: id, idLiga, idUsuario, nombreEquipoFantasy, puntos, fechaCreacion, activo
Opcionales: ninguno

Ejemplo
final p = ParticipacionLiga(
  id: "P1",
  idLiga: "L1",
  idUsuario: "U1",
  nombreEquipoFantasy: "Titanes FC",
  puntos: 0,
  fechaCreacion: 1700000000000,
  activo: true,
);

Modelo: EquipoReal (equipo_real.dart) (nuevo en mod0013)
Constructor
const EquipoReal({
  required this.id,
  required this.idLiga,
  required this.nombre,
  required this.descripcion,
  required this.escudoUrl,
  required this.fechaCreacion,
  required this.activo,
});

Métodos

factory EquipoReal.desdeMapa(String id, Map<String, dynamic> datos)

Map<String, dynamic> aMapa()

EquipoReal copiarCon({ ... })

Campos

Obligatorios: id, idLiga, nombre, descripcion, escudoUrl, fechaCreacion, activo
Opcionales: ninguno

Ejemplo
final er = EquipoReal(
  id: "ER1",
  idLiga: "L1",
  nombre: "Boca Juniors",
  descripcion: "Club Atlético Boca Juniors",
  escudoUrl: "",
  fechaCreacion: 1700000000000,
  activo: true,
);

Modelo: EquipoFantasy (equipo_fantasy.dart) (nuevo en mod0013)
Constructor
const EquipoFantasy({
  required this.id,
  required this.idUsuario,
  required this.idLiga,
  required this.nombre,
  required this.fechaCreacion,
  required this.activo,
});

Métodos

factory EquipoFantasy.desdeMapa(String id, Map<String, dynamic> datos)

Map<String, dynamic> aMapa()

EquipoFantasy copiarCon({ ... })

Campos

Obligatorios: id, idUsuario, idLiga, nombre, fechaCreacion, activo
Opcionales: ninguno

Ejemplo
final ef = EquipoFantasy(
  id: "EF1",
  idUsuario: "U1",
  idLiga: "L1",
  nombre: "Los Pumas",
  fechaCreacion: 1700000000000,
  activo: true,
);

Modelo: Jugador (jugador.dart)

(sin cambios respecto a mod0012)

Constructor
const Jugador({
  required this.id,
  required this.idEquipo,
  required this.nombre,
  required this.posicion,
  required this.nacionalidad,
  required this.dorsal,
  required this.fechaCreacion,
  required this.activo,
});

Métodos

factory Jugador.desdeMapa(String id, Map<String, dynamic> datos)

Map<String, dynamic> aMapa()

Jugador copiarCon({ ... })

Modelo: Alineacion (alineacion.dart)

(sin cambios de contenido, solo cambio de estilo documental)

Constructor
const Alineacion({
  required this.id,
  required this.idLiga,
  required this.idUsuario,
  required this.jugadoresSeleccionados,
  required this.formacion,
  required this.puntosTotales,
  required this.fechaCreacion,
  required this.activo,
});

Modelo: Roles (roles.dart)

(sin cambios estructurales)

Enum
enum RolUsuario { usuario, admin }

Métodos

String get valorDB

String get textoVisible

static RolUsuario desdeValorDB(String valor)